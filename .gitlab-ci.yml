image: "archlinux:latest"

before_script:
  - pacman -Syu --needed --noconfirm reuse just

buildbtw-poc:
  stage: check
  trigger:
    include:
      - local: buildbtw-poc/.gitlab-ci.yml

stages:
  - check

reuse:
  stage: check
  script:
    - just licenses
