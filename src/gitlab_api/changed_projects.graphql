query ChangedProjects($after: String, $group: ID!) {
    group(fullPath: $group) {
        projects(sort: ACTIVITY_DESC, after: $after, includeArchived: false) {
            nodes {
                path
                lastActivityAt
            }
            pageInfo {
                endCursor
                hasNextPage
            }
        }
    }
}
